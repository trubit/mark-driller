apiVersion: v1
kind: ServiceAccount
metadata:
  name: markdriller-sa
  namespace: markdriller
  labels:
    app.kubernetes.io/name: markdriller
    app.kubernetes.io/component: service-account
# Defense-in-depth: Disable automatic token mounting to eliminate credential theft vectors
automountServiceAccountToken: false
